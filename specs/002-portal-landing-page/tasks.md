# Tasks: Unique Portal Landing Page

## Phase 1: Structure Reorganization
- `[ ]` TASK-1: Rename existing `index.html` to `abacus.html`.
- `[ ]` TASK-2: Ensure `abacus.html` works correctly at its new path.

## Phase 2: Portal Foundation
- `[ ]` TASK-3: Create new `index.html` file for the portal.
- `[ ]` TASK-4: Create `portal.css` with dark theme, glassmorphism tokens, and animated gradient mesh background.
- `[ ]` TASK-5: Create `portal.js` to handle dynamic rendering.

## Phase 3: Unique UI & Data Integration
- `[ ]` TASK-6: Create `journeys.js` exporting an array of journey objects (one active, two coming soon).
- `[ ]` TASK-7: In `portal.js`, write render logic to inject journey cards into the `index.html` grid.
- `[ ]` TASK-8: Add hover effects and micro-interactions to the cards in `portal.css` to enhance the "unique vibes".
- `[ ]` TASK-9: Add an animated title and tagline to the portal hero section.
